namespace Xendor.CommandModel
{
    /// <summary>
    /// Domain logic (aka business logic, business rules, and domain knowledge) is the logic that makes business-critical decisions. It’s important to separate domain logic from other types of logic as it helps keep the overall code base simpler.
    /// </summary>
    public interface IDomainLogic
    { }
}